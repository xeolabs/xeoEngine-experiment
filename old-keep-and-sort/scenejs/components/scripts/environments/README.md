Scripts which set up different "environments", ie. base scene graph structure,
camera control, culling systems etc.

These are included by other scripts which do things like add objects to the scene graph etc.